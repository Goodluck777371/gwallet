
// Import and re-export from the hooks directory
import { useToast, toast, ToastFunctions, ToasterToast } from "@/hooks/use-toast";

export { useToast, toast, type ToastFunctions, type ToasterToast };
